# PRIMARY environment for the transport benchmark on CSCS Alps / Santis (GH200, aarch64).
# Two pin files: this is the jax 0.11.1 pin; requirements-santis-jax062.txt is the laptop-
# proven fallback if 0.11.1 misbehaves on Santis.
#
# Python 3.12 or 3.13 (jaxlib / jax-cuda12-plugin 0.11.1 publish cp312-cp313 wheels).
#
#   uv pip install -r requirements-santis.txt
#   uv pip install -e /abs/path/to/gt4py

jax[cuda12]==0.11.1
numpy

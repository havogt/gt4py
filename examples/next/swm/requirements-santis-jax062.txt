# FALLBACK environment for the transport benchmark on CSCS Alps / Santis (GH200, aarch64).
# Two pin files: this is the laptop-proven jax 0.6.2 pin, in case 0.11.1
# (requirements-santis.txt) misbehaves on Santis.
#
# Python 3.10-3.13 (jax 0.6.2 wheels: cp310-cp313; on aarch64 only the CUDA 12 plugin exists).
#
#   uv pip install -r requirements-santis-jax062.txt
#   uv pip install -e /abs/path/to/gt4py

jax[cuda12]==0.6.2
numpy
